// Package config owns versioned configuration models and configuration
// persistence boundaries.
//
// It is intentionally independent of Wails and React. Future phases add TOML
// encoding, decoding, defaults, and atomic writes here.
package config
